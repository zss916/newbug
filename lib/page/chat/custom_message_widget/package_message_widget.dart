import 'package:flutter/material.dart';

class PackageMessageWidget extends StatelessWidget {
  const PackageMessageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
